# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: booking.spec.ts >> Booking Flow >> should render home page and allow searching for a transfer
- Location: e2e\booking.spec.ts:4:7

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: locator('h1')
Expected substring: "Transfers in Barcelona"
Received string:    "Traslados privados en Barcelona con precio cerrado"
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for locator('h1')
    13 × locator resolved to <h1 class="max-w-4xl text-4xl font-semibold leading-tight sm:text-6xl lg:text-7xl">Traslados privados en Barcelona con precio cerrado</h1>
       - unexpected value "Traslados privados en Barcelona con precio cerrado"

```

```yaml
- heading "Traslados privados en Barcelona con precio cerrado" [level=1]
```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test.describe('Booking Flow', () => {
  4  |   test('should render home page and allow searching for a transfer', async ({ page }) => {
  5  |     await page.goto('/es');
  6  |     
  7  |     // Check main title
> 8  |     await expect(page.locator('h1')).toContainText('Transfers in Barcelona');
     |                                      ^ Error: expect(locator).toContainText(expected) failed
  9  |     
  10 |     // Fill origin and destination (simulated)
  11 |     // We would need to mock Google Maps for this, so just checking UI presence
  12 |     const originInput = page.getByPlaceholder(/origen/i);
  13 |     const destinationInput = page.getByPlaceholder(/destino/i);
  14 |     
  15 |     await expect(originInput).toBeVisible();
  16 |     await expect(destinationInput).toBeVisible();
  17 |     
  18 |     // Verify booking button exists
  19 |     const searchBtn = page.getByRole('button', { name: /buscar/i });
  20 |     await expect(searchBtn).toBeVisible();
  21 |   });
  22 | 
  23 |   test('should load the booking checkout page properly', async ({ page }) => {
  24 |     // Navigate directly with query params to simulate search
  25 |     await page.goto('/es/booking?origin=Airport&originId=id1&destination=Hotel&destinationId=id2&date=2028-01-01&time=10:00');
  26 |     
  27 |     // The page should eventually show pricing or a skeleton
  28 |     await expect(page.getByText('Resumen de tu Viaje')).toBeVisible({ timeout: 10000 });
  29 |   });
  30 | });
  31 | 
```